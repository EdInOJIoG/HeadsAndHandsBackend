public class Monsters extends Entity {

    public Monsters(int attack, int defence, int healthPoints, int minDamage, int maxDamage) {
        super(attack, defence, healthPoints, minDamage, maxDamage);
    }
}
